#ifndef SLIYANIE2_HPP
#define SLIYANIE2_HPP

#include <vector>

namespace ban {
    void merge(std::vector<int>& arr, int left, int mid, int right);
    void merge_sort(std::vector<int>& arr, int left, int right);
}

#endif